{
  "buildCommand": "pnpm run docs:build && pnpm run postdocs:build",
  "outputDirectory": "docs/dist",
  "rewrites": [
    {
      "source": "/:match*",
      "destination": "/:match/overview"
    }
  ]
}
