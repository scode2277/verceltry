name: 🥢 Spell check 

on:
  pull_request:
    branches:
      - main
jobs:
  typo_check:
    name: 🥢 Spell check
    runs-on: ubuntu-latest
    permissions:
      contents: read
      pull-requests: write
      issues: write
    env: 
      TYPOS: ""
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          ref: ${{ github.event.pull_request.head.sha }}
          fetch-depth: 2

      - name: Find and check typos in Markdown/MDX files
        id: find_typos
        run: |
          echo "Checking for typos..."
          set -o pipefail
          # Run cspell and capture output, but don't fail yet
          output=$(npx cspell lint "docs/pages/**/*.{md,mdx}" --no-progress --no-summary || true)

          if [[ -n "$output" ]]; then
            echo "$output"
            # Format output: group by file like aspell version
            formatted=$(echo "$output" | sed 's/^/    1. /')
            TYPOS+="- 📄 docs/pages:"
            TYPOS+=$'\n'
            TYPOS+="$formatted"
            TYPOS+=$'\n'
            {
              echo 'TYPOS<<EOF'
              echo "$TYPOS"
              echo EOF
            } >> "$GITHUB_ENV"
          fi

      - name: Comment on pull request
        if: env.TYPOS != ''
        uses: actions/github-script@v7
        with:
          github-token: ${{ secrets.GITHUB_TOKEN }}
          script: |
            const author = '${{github.event.pull_request.user.login}}';
            const typos = `${{ env.TYPOS }}`;
            const body = `
            Hi @${author},

            Following typos were found in the pull request:
            
            ${typos}

            ## ℹ️ Here's how to fix them:
            - **Fix typos:** Open the relevant files and fix any identified typos.
            - **Update wordlist:** If a flagged word is actually a project-specific term add it to \`wordlist.txt\` in the project root.
              Each word should be listed on a separate line.
            - **🚧 Remember:**
              - When adding new words it MUST NOT have any spaces or special characters within or around it.
              - \`wordlist\` is NOT case sensitive.
              - Use backticks to quote code variables so as to not bloat the \`wordlist\`.
            `;

            github.rest.issues.createComment({
              issue_number: context.issue.number,
              owner: context.repo.owner,
              repo: context.repo.repo,
              body: body
            });

            core.setFailed('🥢 Spell check: Typos found in docs. Please fix them.');
